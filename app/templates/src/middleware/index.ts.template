
import {
    handleCors,
    handleBodyRequestParsing,
    handleCompression,
    handleFileUpload,
    handleHelmet,
    handleMorgan,
} from "./common";

export default [handleCors, handleHelmet, handleMorgan, handleBodyRequestParsing, handleCompression, handleFileUpload];